import { ShowMarkerWindowInfoDirective } from './show-marker-window-info.directive';

describe('ShowMarkerWindowInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMarkerWindowInfoDirective();
    expect(directive).toBeTruthy();
  });
});
